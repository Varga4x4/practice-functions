// Create a function which will coordinates of 2 points. Each cordinate has x and y keys. Funxtion should return difference between these points as object {x: ?, y: ?}

const getCoordinates = (x, y) => {

    const coordinates = {
        x: x,
        y: y
    }

    console.log(coordinates)
}
getCoordinates(4, 3)